import { ResponsesOfQuestionPipe } from './responses-of-question.pipe';

describe('ResponsesOfQuestionPipe', () => {
  it('create an instance', () => {
    const pipe = new ResponsesOfQuestionPipe();
    expect(pipe).toBeTruthy();
  });
});
